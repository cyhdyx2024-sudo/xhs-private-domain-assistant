# 小红书私信智能顾问 V1.0 产品报告

> 历史快照：本文用于保留 V1.0 决策依据，不代表当前 V1.1.0 的发布状态。最新工程与实机结论见 [`docs/CODE_REVIEW.md`](docs/CODE_REVIEW.md)。

报告日期：2026-08-28  
产品状态：V1.0 已发布，适合受控试用，不适合立即公开规模化售卖

## 一、结论

这不是一个已经成熟的“无人销售 Agent”，而是一个具备真实业务价值的“小红书私信客服副驾”。

当前最合适的路径是：先作为有人监督的半自动工具，服务 3～5 个真实专业号，验证回复质量、错发风险和节省时间；暂时不要继续增加飞书、更多模型或复杂知识库功能。

产品阶段判断：

- 内部自用：可以使用
- 设计伙伴试用：有条件可以
- 小范围收费试点：完成真实回归后可以
- 公开自助售卖：暂不建议
- 无人值守全自动：暂不建议

## 二、产品定位

### 目标用户

- 使用小红书专业号客服工作台的商家或销售团队
- 每天存在重复咨询、夜间咨询或多轮跟进
- 已有基本业务资料，但缺少稳定客服话术和持续值守能力
- 愿意先采用半自动模式，而不是一开始完全托管

### 核心问题

1. 客服回复慢，夜间线索容易流失。
2. 重复问题占用人工时间。
3. 不同客服的表达和事实口径不一致。
4. 通用大模型不了解当前客户上下文，容易答非所问或编造动作状态。

### 当前价值主张

在不改变小红书原有工作台的前提下，读取当前会话上下文，生成可审核的回复草稿；对高风险和无法核验的问题主动停下，减少重复劳动，同时保留人工控制权。

## 三、V1.0 已具备能力

- Chrome Manifest V3 扩展
- 半自动生成与预填，默认不自动发送
- 有开关、有冷却和发送后回读的全自动模式
- 多轮上下文与会话隔离
- BYOK 多模型接入
- 业务资料检索和人工优质案例学习
- 线索识别与导出
- 隐藏虚拟列表隔离
- “加了没、发了吗”等不可核验状态自动暂停并转人工
- 工作区令牌使用请求头传输，服务日志自动遮蔽旧式查询参数令牌
- 模型 Key 不写入云端数据库

## 四、已经验证的事实

### 工程验证

- 扩展版本已统一为 `1.0.0`。
- 前端发布契约测试和 JavaScript 语法检查通过。
- 后端 5 项测试通过，覆盖反馈知识、外部动作状态识别、BYOK 必填和错误回复拦截。
- 生产 Bridge 服务处于运行状态，`/healthz` 返回 200 且为产品模式。
- GitHub 私有仓库、`v1.0.0` 标签和正式 Release 已创建。
- GitHub Release 下载回读后的 SHA-256 与本地包一致。
- 官网和 `latest.zip` 已同步到 V1.0.0，线上包与本地包哈希一致。

### 真实页面验证

- 在真实小红书专业号私信页面发现并复现了隐藏虚拟列表问题。
- 新的筛选逻辑能排除隐藏会话卡片，并选择真实可滚动的联系人列表。
- 验证过程中没有开启全自动，也没有向真实客户发送测试消息。

## 五、仍未完成的关键验证

以下项目不能由单元测试代替：

1. Chrome 重新加载 V1.0 后的完整端到端回归。
2. 至少 20 条真实会话的半自动生成质量。
3. 人工修改率、答非所问率和事实错误率。
4. 页面切换、虚拟滚动、多人连续进线时是否发生串会话或重复发送。
5. 小红书页面改版后的持续兼容性。
6. 不同模型厂商的错误码、超时和限流行为。
7. 旧工作区令牌的吊销与客户侧恢复流程。

因此，目前不能宣称“已经可以稳定无人值守”或“能够提升多少转化率”。

## 六、主要风险

### P0：错发和事实错误

一旦向错误客户发送、重复发送，或虚构“已经加你、已经发送”等外部动作，损失会直接落到真实业务。V1.0 已增加拦截，但仍需真实样本验证。

### P0：平台 DOM 依赖

扩展依赖小红书网页结构。平台改版可能让联系人识别、消息读取或发送按钮定位失效。需要版本监控和快速停用机制。

### P1：凭据与租户管理

当前已经修复令牌出现在 URL 和日志的问题，但曾暴露的旧测试令牌仍应吊销。公开商用前还需要工作区找回、令牌轮换、租户停用和操作审计。

### P1：产品运维能力不足

现在主要依靠日志和本地界面判断运行状态。缺少面向运营人员的租户状态、错误聚合、模型成本和版本分布看板。

### P1：合规与平台政策

自动回复、客户信息处理、知识资料上传和模型转发需要明确隐私说明、数据保留规则、服务条款和客户授权边界。

### P2：功能过多导致主链失焦

知识库、飞书导入、反馈学习和多模型都有价值，但现阶段最大的未知不是功能数量，而是回复是否稳定、是否节省人工、是否会误伤真实客户。

## 七、建议的试点验收线

以下是建议阈值，不是当前已达到的数据：

- 真实试点账号：3～5 个
- 每个账号半自动观察：至少 20 条有效会话
- 串会话、错客户、重复发送：0 次
- 外部动作状态自动拦截：100%
- 事实性严重错误：0 次
- 回复可直接采用或仅需轻微修改：≥ 85%
- 人工大幅重写比例：≤ 20%
- 典型回复端到端耗时：P95 ≤ 15 秒
- 连续稳定观察：至少 7 天

任何一次串会话、错客户或不可恢复的重复发送，都应停止扩大试点并回到根因排查。

## 八、未来 14 天最优行动

### 第 1～2 天：完成真实回归

- 在 Chrome 扩展页重新加载 V1.0。
- 保持全自动关闭。
- 用真实专业号完成页面切换、生成、预填、人工修改和发送后回读测试。

### 第 3～7 天：半自动设计伙伴试用

- 选择 3 个会话量适中的账号。
- 记录原始客户消息、模型草稿、人工最终回复、修改原因和响应耗时。
- 每天只修复最高频、最高损失的问题。

### 第 8～10 天：有限全自动验证

- 只对白名单意图开放，例如问候、营业时间和已确认的标准问答。
- 价格、承诺、投诉、退款、外部动作状态继续转人工。

### 第 11～14 天：决定是否收费

- 如果达到验收线，采用“部署/配置服务费 + 月度值守费”的托管试点，而不是立即做自助 SaaS。
- 如果没有达到，停止扩功能，集中解决人工大幅重写和事实错误的头部原因。

## 九、商业化判断

V1.0 最适合卖的不是“AI 自动回复软件”，而是“有人监督的小红书私信响应提效服务”。

早期采用托管式试点更合理：你可以控制账号配置、知识资料、模型选择和自动化边界，也能直接收集回复修改数据。等到部署、恢复、计费和错误监控稳定后，再考虑自助 SaaS。

建议先证明三个结果：

1. 客服平均处理时间是否下降。
2. 夜间和高峰期首响是否改善。
3. 在不增加错发和事实错误的前提下，人工修改量是否下降。

如果这三个结果没有被真实数据证明，继续增加功能不会让产品成立。

## 十、最终判断

V1.0 已经从“不断叠版本的原型”收敛成一个可以进入真实试点的产品，但它的产品价值尚未被用户数据证明。

下一步不是开发 V1.1，而是完成一次受控、可记录、能得出结论的真实试点。
