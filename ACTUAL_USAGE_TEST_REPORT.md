# 小红书私信智能顾问 V1.0 实际使用测试报告

测试日期：2026-08-28  
测试环境：真实小红书专业号私信工作台、线上 Bridge、V1.0 Chrome 扩展  
安全边界：全自动始终关闭；未点击发送；未产生新的真实客户消息

## 一、结论

V1.0 已能真实加载，会话身份切换和虚拟消息节点过滤有效，但仍不具备开放全自动值守的条件。

当前阻断项不是视觉或功能数量，而是配置失效无法自愈、接口失败高频重试、旧消息没有年龄上限、外部动作承诺拦截不完整，以及全列表巡检缺少独立进度和无效对象排除。

## 二、实际通过的项目

- 页面显示 `V1.0`，执行轨迹显示 `V1.0.0 已加载`。
- 总开关关闭时不会生成或发送回复。
- 点击不同联系人后，控制台“当前客户”能同步变化；恢复原会话后名称正确。
- 真实消息 DOM 中存在 `translateY(-9999px)` 的旧复用节点，V1.0 规则能排除这些节点。
- 当前会话的客户/客服角色和时间顺序识别正确。
- 只有系统温馨提示、没有客户正文的会话会得到空历史，不会直接生成回复。
- 联系人列表可以真实滚动：可视高度约 409px，总滚动高度约 2076px。
- 从顶部逐屏滚到底，共覆盖 33 个独立会话，证明页面具备完整巡检的物理条件。

## 三、P0 阻断问题

### 1. 失效工作区 Token 无法自愈

实际表现：知识库返回 `workspace_token_invalid`，回复接口返回 HTTP 401。

根因：设置页仅在本地完全没有 Token 时注册工作区。只要本地存在一个失效 Token，就会永久复用它。

更严重的是，保存业务配置时没有检查 `/tenant/config` 的响应状态。即使服务端返回 401，页面仍会保存本地配置并可能显示“配置已保存”。

### 2. 半自动失败后形成请求风暴

实际表现：临时开启半自动后，约 20 秒出现 20 多次 HTTP 401。执行轨迹几乎每秒重复“分析会话 → 大模型暂时不可用”。

根因：副驾轮询和页面 MutationObserver 会不断重新调度相同消息；失败后没有按消息签名设置退避、熔断或自动暂停。

建议：401/403 立即关闭总开关并提示重新配置；429、5xx 和网络错误采用指数退避；同一消息失败后至少 60 秒内不得重试。

### 3. 全自动没有消息年龄上限

实际列表仍包含 7 月和 8 月中旬的“超时未回复”。当前逻辑只看未读/超时标记，不检查客户最后消息时间。

这会导致启用全自动后补回数天甚至数周前的旧会话。

建议：全自动默认只处理最近 2 小时的客户消息；2～24 小时进入人工待办；超过 24 小时不自动处理。

### 4. 外部动作虚假承诺拦截不完整

真实历史回复曾出现：“好的，我这就添加兼一老师的企微，您那边看下有收到申请没？”

当前质检能拦截客户追问“加了没”时谎称已经完成，但不能拦截以下主动承诺：

- 我这就添加
- 我马上加您
- 稍后把资料发过去
- 我帮您通过申请

使用现有 `reply_quality_issues` 实测，上述回复在“麻烦添加下这个”“加V详聊”场景下均未报错。

### 5. 巡检与回复队列耦合

V1.0 只有在当前已渲染 DOM 中找不到待处理卡片时才向下滚动。单个失败、空会话或状态问题会延迟继续巡检。

`autoSkipUntil` 使用“会话 ID + 卡片预览”作为键；实际卡片预览可能长期停留在系统提示，而不是客户最新正文。客户发送新消息后，旧的跳过状态可能继续生效，造成漏回。

建议：独立维护巡检游标和已扫描 UID；先完成一轮列表覆盖，再处理回复队列。跳过状态应绑定真实客户消息签名，而不是卡片预览。

## 四、P1 使用与逻辑问题

### 1. 无效联系人没有排除

完整滚动后发现 5 个待回复标记：

- 1 个真实客户会话
- 1 个只有系统温馨提示的会话
- 3 个“用户已注销”会话

三个已注销会话分别来自 07/13、08/17、08/17，当前逻辑仍会周期性点击并解析它们。

需要默认排除：用户已注销、平台系统通知、企业员工会话，以及用户配置的黑名单。

### 2. 模型供应商 UI 与 Bridge 允许列表不一致

设置页提供 DeepSeek、OpenAI、阿里云百炼、智谱、Moonshot、硅基流动、火山、MiniMax 和 OpenRouter。

Bridge 默认只允许：OpenAI、DeepSeek、硅基流动和 OpenRouter。其余界面选项即使模型直连测试通过，运行时仍可能被 Bridge 拒绝。

### 3. “测试模型”没有测试真实产品链路

设置页的模型测试直接从浏览器请求模型厂商，不经过工作区鉴权和 Bridge。因此可能出现“模型测试成功，但实际回复持续 401/400”。

应增加一项真正的端到端测试：工作区 Token → Bridge → 模型厂商 → 返回测试回复。

### 4. 设置页部分接口失败会伪装成空数据

FAQ、CRM 线索和人工反馈列表没有统一检查 HTTP 状态。401 等错误可能被显示为“暂无数据”，让用户误以为只是没有内容。

### 5. 弹窗没有强制首次设置门禁

弹窗读取不到 `enabled` 时按开启显示，也没有使用 `onboardingComplete` 禁止全自动。未配置用户可能看到“运行中”，实际页面则持续报鉴权错误。

### 6. 全自动会干扰正在阅读的客服

当前只在用户最近 3.5 秒有点击或键盘操作时暂停。客服如果只是阅读对话超过 3.5 秒，扩展就可能自动滚动列表、切换会话或开始输入。

需要更长的人工占用窗口、明确的“离开值守”按钮，或扫描过程不改变操作员当前视图。

### 7. “今日应答”不是今日数据

`repliedCount` 和 `leadsCount` 没有按日期重置，弹窗却标为“今日应答/捕获客资”。实际显示的是累计值。

### 8. 12MB 上传上限与请求体上限冲突

扩展允许 12MB 文件，但 Base64 后体积约增加三分之一，再加 JSON 包装会超过 Bridge 的 16MB 请求上限。接近 12MB 的合法文件可能在服务端直接失败。

### 9. 缺少配置迁移和版本升级提示

从不同目录重新加载未打包扩展会产生新的扩展 ID，旧配置不会迁移。页面没有明确提示“当前配置为空/Token 已失效/需要重新注册”。

### 10. 初次加载客户名称显示滞后

页面刷新后，即使已有选中会话，控制台仍显示“当前客户：识别中”；只有再次点击联系人后才更新。

## 五、巡检实测数据

- 列表顶部初始渲染：21 个会话节点
- 逐屏滚动位置：0、389、778、1167、1556、1607
- 完整覆盖：33 个独立会话
- 带待回复标记：5 个
- 有真实可回复正文：至少 1 个
- 只有系统提示：1 个
- 已注销空会话：3 个

这说明“上下滚动后能看到完整列表”是成立的，但当前产品缺少可验证的自动巡检闭环。控制台至少应显示：本轮已扫描数量、总覆盖数量、当前位置、最后完整巡检时间、跳过原因统计。

## 六、当前上线判断

- 半自动真实试用：修复 Token 自愈和失败熔断后再继续
- 小范围人工监督试点：暂缓
- 全自动值守：禁止开启
- 公开收费：暂不具备条件

修复顺序：

1. Token 自愈、保存响应检查、401 自动停机
2. 同消息退避和全局熔断
3. 消息年龄上限、已注销/系统/平台员工排除
4. 独立巡检游标和覆盖率显示
5. 外部动作承诺质检
6. 供应商列表、端到端测试和配置迁移
